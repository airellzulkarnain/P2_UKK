[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = {extras = ["all"], version = "*"}
python-jose = {extras = ["encryption"], version = "*"}
passlib = {extras = ["bcrypt"], version = "*"}
fpdf2 = "*"

[dev-packages]

[requires]
python_version = "3.10"
